# Platform Credentials Configuration
# NOTE: This file stores encrypted credentials
# Use credential_manager.py to encrypt and store sensitive data

# Instagram Credentials
instagram:
  username: ""  # Set via CLI or environment variable
  password: ""  # Encrypted password (set via credential_manager)
  encrypted: false  # Set to true after encryption

# YouTube Credentials
youtube:
  email: ""  # Set via CLI or environment variable
  password: ""  # Encrypted password (set via credential_manager)
  encrypted: false  # Set to true after encryption

# TikTok Credentials
tiktok:
  username: ""  # Set via CLI or environment variable
  password: ""  # Encrypted password (set via credential_manager)
  encrypted: false  # Set to true after encryption

# GitHub Models API
github_api:
  token: ""  # Set via environment variable GITHUB_TOKEN
  encrypted: false  # Set to true after encryption

# Encryption Settings
encryption:
  # Key file location (generated by credential_manager)
  key_file: "cache/.encryption_key"
  # Use environment variable for encryption key
  use_env_key: true
  # Environment variable name for encryption key
  env_key_name: "ENCRYPTION_KEY"
